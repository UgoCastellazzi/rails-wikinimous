<ul>
  <% @articles.each do |article|%>
    <li>
      <h2><%= link_to article.title, article_path(article.id)%></h2>
      <h3> <%= article.content.slice(0, 100) %>...</h3>
    </li>
  <% end %>
</ul>

<%= link_to "New article", new_article_path%>
