<ul>
  <% @articles.each do |article|%>
    <li>
      <%= link_to article_path(article.id) do %>
        <div>
            <h2><%= article.title %></h2>
            <h3> <%= article.content.slice(0, 100) %>...</h3>
        </div>
      <% end %>
    </li>
  <% end %>
</ul>
